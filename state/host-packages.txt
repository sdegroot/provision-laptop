# rpm-ostree layered packages
# Keep this list minimal — prefer Flatpak and Toolbox over host packages.

# Essential tools
vim-enhanced
htop
git

# Firmware and hardware
fwupd

# Shells
zsh

# CLI tools
gh
fzf

# Tuxedo hardware (fan control, keyboard backlight, EC interface — x86_64 only)
# Uses sdegroot/tuxedo-drivers-kmod COPR (akmod build) instead of official
# tuxedo-drivers (DKMS) which is incompatible with rpm-ostree's bwrap sandbox.
# akmod-tuxedo-drivers compiles kernel modules during rpm-ostree transaction.
[x86_64] akmod-tuxedo-drivers
[x86_64] tuxedo-drivers-kmod-common
[x86_64] tuxedo-control-center

# YubiKey
libfido2
yubikey-manager
pam-u2f

# Fingerprint reader
fprintd
libfprint

# Sensors
lm_sensors

# Terminal
ghostty
jetbrains-mono-fonts

# Fonts
google-noto-sans-fonts

# Podman
podman-compose

# Power management (tuned-ppd is in F43 base image, replaces power-profiles-daemon)

# GNOME extensions
gnome-shell-extension-gsconnect

# Netbird VPN
netbird
