# Container definitions
# Format: name:build_context:description
#
# name — container name/tag
# build_context — path to directory containing Containerfile (relative to repo root)
# description — human-readable description

ai-sandbox:containers/ai-sandbox:Isolated AI/ML sandbox environment
