#!/usr/bin/env bash
# bin/check - Verify system matches desired state.
#
# Usage: bin/check [--module <name>]

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../lib/engine.sh"

log_info "Checking desired state..."
parse_engine_args "check" "$@"
