# Blacklist unused ACP70 ASoC driver — no matching machine driver exists
# for this laptop's Conexant SN6140 HDA codec. Audio is handled correctly
# by snd_hda_intel + snd_hda_codec_conexant.
blacklist snd_acp_pci
