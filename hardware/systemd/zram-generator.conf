# Override default zram: 16GB with zstd for better compression ratio.
# With 93GB RAM this provides ~50-60GB effective swap-in-RAM before
# the NVMe swapfile is touched.
[zram0]
zram-size = 16384
compression-algorithm = zstd
