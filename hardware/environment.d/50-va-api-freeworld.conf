# Point libva to the freeworld VA-API drivers (mesa-va-drivers-freeworld)
# Without this, libva only searches /usr/lib64/dri/ which lacks the
# freeworld radeonsi driver needed for AMD hardware video decoding.
LIBVA_DRIVERS_PATH=/usr/lib64/dri-freeworld
