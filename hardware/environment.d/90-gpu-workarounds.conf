# Workaround for AMD GPU rendering artifacts with fractional scaling
# Forces Mutter's KMS thread to run in user mode, avoiding race conditions
# during compositor redraws (popups, overlays) on RDNA GPUs.
MUTTER_DEBUG_KMS_THREAD_TYPE=user
