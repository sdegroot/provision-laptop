# Testcontainers configuration — use Podman via podman machine
# docker.host is intentionally unset so Testcontainers falls through to Docker
# context auto-detection (same path the docker CLI uses). Hard-coding the
# podman socket is fragile because its tempdir path changes across reboots.
testcontainers.ryuk.disabled=true
