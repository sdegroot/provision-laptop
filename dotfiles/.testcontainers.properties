# Testcontainers configuration for Podman
#
# NOTE: The socket path assumes UID 1000 (typical for first/only user on a
# personal laptop). If your UID differs, update the path below.
# Check with: id -u
docker.host=unix:///run/user/1000/podman/podman.sock
testcontainers.ryuk.disabled=true
