[Unit]
Description=SSH agent for git commit signing (key cache)

[Service]
Type=simple
ExecStart=/usr/bin/ssh-agent -D -a %t/ssh-signing-agent.sock
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
